// Directly run the exe
{
    "version":"0.2.0",
    "configurations": [
        {
            "name":"C Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program":"${workspaceRoot}/bin/Debug/Sandbox.exe",
            "args":[],
            "cwd":"${workspaceFolder}",
            "console":"integratedTerminal"
        }
    ]
}
